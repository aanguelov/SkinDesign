

<nav class="navbar navbar-default">
    <div class="container-fluid">
        <div class="navbar-header">
            <a class="navbar-brand" href="/">Skin Design</a>
        </div>
        <div class="collapse navbar-collapse">
            <ul class="nav navbar-nav">
                <li><a asp-controller="Procedures" asp-action="All">Procedures</a></li>
                <li><a asp-controller="Home" asp-action="Contacts">Contacts</a></li>
            </ul>
            @if (User.Identity.IsAuthenticated)
            {
                <div class="nav navbar-nav navbar-right">
                    <span class="navbar-text">Hello, @User.Identity.Name</span>
                    <a asp-controller="Account" asp-action="Logout" class="btn btn-default navbar-btn">Logout</a>
                </div>
            }
        </div>
    </div>
</nav>